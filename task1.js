/***
 * Write a function that returns the count of 
 * distinct case-insensitive characters 
 * that occur more than once in a given string.
 * 
 * Examples
 * "abcde" -> 0 (no characters repeated more than once)
 * "aabbcde" -> 2 ('a' and 'b' repeated more than once)
 * "aaaa" -> 1 ('a' repeated more than once)
 */

const findDuplicates = (inputText) => {
    return 0;
}